import { Typography } from "@mui/material";
import React from "react";

export default function JourniSettings() {
  return (
    <div>
      <Typography variant="h5">Journi Settings</Typography>
      <Typography>platform type: merchant or restauranteur or both</Typography>
    </div>
  );
}
